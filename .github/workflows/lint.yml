name: Lint

on:
  pull_request:
    paths:
      - "agent/**"
      - ".github/workflows/lint.yml"
  push:
    branches: [main]
    paths:
      - "agent/**"
      - ".github/workflows/lint.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Ruff check
        run: pipx run ruff==0.16.0 check agent  # pinned: an unpinned linter turns a green tree red on its own
